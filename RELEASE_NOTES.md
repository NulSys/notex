Manual ordering, seamless header, and a refined background.

- **Drag to reorder notes** — set the sort to "Manual (drag)" and drag notes into any order you like.
- **Seamless Settings header** — the title and ✕ now float directly on the window background (no header bar), staying put while the body scrolls.
- **Refined background gradient** — the theme color now shows at the top, with the accent gradient easing in toward the bottom.
- Also enables in-app drag & drop generally (so dragging an image into the "Notes from image" box works too).
