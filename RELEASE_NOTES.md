Editor scrolling — really fixed this time.

- **The markup/editor pane now scrolls** in split view (and everywhere). Verified: the editor's internal wrapper wasn't height-constrained, so CodeMirror grew with the content instead of scrolling.
