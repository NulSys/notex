Catches up everything from 0.3.6, plus a lock-screen tweak.

- **Drag to reorder notes** — set the sort to "Manual (drag)" and drag notes into any order.
- **Seamless Settings header** — the title and ✕ float on the window background (no header bar), staying put while the body scrolls.
- **Refined background gradient** — the theme color shows up top, with the accent gradient easing in toward the bottom.
- **Muted lock screen** — the unlock screen's gradient now matches the app's subtle look.
- Enables in-app drag & drop generally (so dragging an image into "Notes from image" works too).
